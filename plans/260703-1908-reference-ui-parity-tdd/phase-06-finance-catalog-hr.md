---
phase: 6
title: "Finance + catalog + HR"
status: completed
effort: "XL"
---

> **Completed 2026-07-07.** Gate clean: `test` 369 pass (deterministic ×2), fresh `tsc --noEmit`, `lint` (0 errors), `build` all green. Main session first landed the shared foundations: 3rd branch `Cộng tác viên tuyến huyện` (V1, only BranchMapModal coords needed the extra key), CrudTablePage extension (opt-in bulkDelete/saveAndNew/export/addLabel + FieldType money/radio, page sizes 20-300), CrudSheet Lưu & Thêm mới + radio/money fields. Then 3 parallel agents: catalog+customer (C1-C14 + CU1, KhuVuc re-modeled to Tỉnh/Quận/Xã, product-editor, 2 customer modals), HR (H1-H4 new pages + H5-H10 + employee editor 28-field, static payroll per Finding 12), finance (F1-F3 + invoice composer, Công nợ per-ticket receivables with the wall-clock overdue path DELETED). One agent (catalog) hit a mid-run API timeout; main session finished it — fixed a pre-existing latent circular-import in the masterdata barrel by repointing all 19 `*.mock.ts` to a standalone `make-mock-api.ts`, plus 4 unsafe casts + 1 impossible-comparison test. Invented fields (maKH/tongPhieu/nuocSX/hanTT/soNgay/Trạng thái) grep-clean.

# Phase 6: Finance + catalog + HR

## Overview

Correct the data models and column sets of the finance (3 pages), catalog (14 pages), customer (1 page), and HR (10 pages) sections to reference parity. Replaces invented columns/taxonomies with the verified reference schemas (12-type ChungTu, per-ticket Công nợ, VAT-invoice composer, Tỉnh→Quận→Xã Khu vực/Phường-Xã, product-linked Phí giao, labor-price Lỗi sửa chữa, 9-value Nhóm khách hàng, exception-based Chấm công, 17-column Bảng lương, full employee editor), builds the 4 missing HR stub pages, and applies the cross-cutting CrudTablePage upgrades (bulk-select/delete, `Lưu & Thêm mới`, autocomplete-with-`[+]`-quick-create, Excel export) from the P2 primitives. **Data layer (D5):** these pages read the LIVE `src/mock/finance-mock.ts` + `src/mock/masterdata/*` + their `*.config.ts` — this phase edits those layers in place and wires in the P1 lookup modules (Kỳ, Tỉnh/Quận/Xã + `TUYEN`, 9-value customer taxonomy, 12-type ChungTu, per-ticket Công nợ, Chấm công, Lỗi sửa chữa). It does NOT regenerate any `SEED_*` array (those are deleted in P1).

## Context links

- Spec (contract): `plans/reports/ref-ui-parity-sections/section-finance.md`, `section-catalog-a.md`, `section-catalog-b.md`, `section-customer.md`, `section-hr.md` — read ALL fully incl. their `## Addendum — verified from mirrored partials (260703)` sections, which carry the exact verified column lists + full-page editor specs (Invoice composer, Hàng hóa editor, NhanVien editor). Addenda override pre-addendum "unverified" notes.
- Local code consumed (LIVE layers this phase edits, D5): `src/pages/tai-chinh/*` (3), `src/pages/danh-muc/*` (16), `src/pages/nhan-su/*` (7), `src/config/crud-configs/*` (23), `src/config/finance-tables/{thu-chi,cong-no,hoa-don}.config.ts`, `src/types/{finance-types,masterdata-types}.ts` (owns the live `KhuVuc{maKhuVuc,tenKhuVuc}`), `src/components/crud/CrudTablePage.tsx`, `src/components/finance/*`, `src/constants/routes.ts`, `src/routes/index.tsx`, `src/mock/finance-mock.ts`, `src/mock/masterdata/*`. **P1 lookup modules imported (read-only):** `src/mock/seed/{ky,tinh-quan-xa,nhom-khach-hang,chung-tu,cong-no,cham-cong,loi-sua-chua,phi-giao}.ts`.
- P1 outputs assumed present (as importable lookup modules, not "seed regeneration"): **Kỳ lookup** (required by Ứng lương, Bảng lương, Chấm công×2), **Tỉnh→Quận→Xã + `TUYEN` lookup** (required by Khu vực, Phường/Xã, customer filters — named `TUYEN`, distinct from the live `KhuVuc`, Finding 2), and the taxonomy/relational lookups (9-value customer type, 12-type ChungTu, per-ticket Công nợ, Chấm công exception records, Lỗi sửa chữa labor prices). **Reconcile (Finding 2):** the catalog "Khu vực" page's live entity stays `KhuVuc{maKhuVuc,tenKhuVuc}` in `masterdata-types.ts`; this phase re-models it onto Tỉnh/Quận/Xã ids + Cây số/Tiền công (the reference "Khu vực" = route), consuming P1's Tỉnh/Quận/Xã lookup — the `TUYEN` array is the seed data behind it. Do not create a second `KhuVuc` symbol.
- P2 primitives consumed: DataTable bulk-select extension + `Chọn tất cả` + bulk-delete toolbar, `ServerAutocomplete` + `[+]` quick-create modal, `export-xlsx.ts`, `print-window.tsx`, full-page line-item editor template (Invoice composer), Kỳ picker.

## Requirements

**Exact Vietnamese column headers/labels are normative — build verbatim, in order.** Each item traces to a section-file gap row or verified column list. Cross-cutting: every list below gets bulk-select + `Chọn tất cả` + bulk-delete where the reference has it; every catalog side-form gains `Thêm Mới` / `Lưu` / `Lưu & Thêm mới`; related-entity selects become `ServerAutocomplete` with `[+]` quick-create; rows-per-page options 20/30/50/100/150/200/300.

### Finance (tai-chinh)

- **F1 ChungTu / Thu Chi** (`ThuChiPage`) — columns exact (15): `[☑ Chọn tất cả] | Tình Trạng | Số chứng từ | Loại phiếu | Hình thức | Số Phiếu SC/NK | Kỹ thuật | Đại lý/Trạm | Tên khách hàng | Ngày lập | Số tiền | Nội dung | Người tạo | Người Thu/Chi | Ngày Thu/Chi | Chọn`. **Loại thu chi** 12-value taxonomy (`Phiếu Thu`, `Phiếu Chi`, `Phiếu Thu Sửa Chữa`, `Phiếu Thu Bán Hàng`, `Thu Trả Hàng Lỗi`, `Thu Khác`, `Chi Lương`, `Chi Xăng`, `Chi Trả Hàng`, `Chi Mua Hàng`, `Chi Khác`, `Chi Trả Vận Chuyển`) — column + filter. **Tình trạng** = collection state (`Chưa thu`/`Đã thu`/`Đã thu ngoài`/`Chưa chi`/`Đã chi`), replaces invented approval state. Header buttons **Lập Phiếu Thu** + **Lập Phiếu Chi** (separate voucher create modals). Per-row **in phiếu** print (`.ms-btn-receipt-print`) — only row action. `Số Phiếu SC/NK` links to source (repair → detail route new tab; PNK → receiving detail modal). Filters (~11): Chi nhánh, Tình trạng, Hình thức thu, Loại thu chi, Kỹ thuật autocomplete, NSX autocomplete, Số chứng từ, Số phiếu SC/hãng, Tên khách hàng/ĐT, Nội dung, Người tạo autocomplete, Đại lý autocomplete, **Ngày lập vs Ngày thu/chi radio** (`LoaiNgay`), date range (drives table). KPI boxes (search-scoped): `Doanh thu`+`Doanh thu ngoài` (green), `Phải thu` (blue), `Chi phí` (yellow), `Phải trả` (red). Exports: **Xuất ra Excel** + **Xuất ra Excel Thu SC**. Voucher codes `PTT-`/`PCC-`. Bulk-select.
- **F2 Công Nợ** (`CongNoPage`) — columns exact (10): `Số phiếu | Loại phiếu | Ngày lập | KTV | Số tiền | Đã trả | Còn lại | Tên khách hàng | Điện thoại | Chọn`. **NO checkbox column** (verified). Re-model as per-ticket receivables (from Phiếu sửa chữa / Phiếu bán hàng), replace generic ledger. `Chọn` action = **thanh toán** (settle-debt → creates thu voucher). Filters: Chi nhánh, **Loại thanh toán** (`Phiếu sửa chữa`/`Phiếu bán hàng`), Số phiếu, Tên/ĐT khách hàng autocomplete, Kỹ thuật autocomplete, **Tất cả / Theo ngày** toggle + date range. No create button, no KPI strip, no due-date/Hạn TT concept (remove invented).
- **F3 Invoice / Hóa Đơn** (`HoaDonPage`) — columns exact (10): `[☑ Chọn tất cả] | Số Hóa Đơn | Hình Thức Thanh Toán | Khách Hàng | Mã Số Thuế | Tiền thuế | Tổng Thanh Toán | Ngày Lập | Người Lập | Chọn`. Add MST + Hình Thức Thanh Toán + Người Lập to model. **Xóa** bulk delete (checkbox + select-all + red toolbar btn). Header **Lập Hóa Đơn** → full-page composer. Filters: Số hóa đơn, Mã số thuế, **Hình thức thanh toán** (`Tiền mặt`/`Chuyển khoản`), Tên đơn vị, date range. Remove invented Trạng thái column.
- **F3b Invoice composer** (`/tai-chinh/hoa-don/tao-moi`) — full-page per verified `page-Invoice-Create`. Toolbar `Lưu` / `Lưu & Thêm mới` / `In Hóa Đơn` / `Danh sách hóa đơn`. Fieldset **Thông tin khách hàng**: Số hóa đơn*, Ngày xuất* (default today), Tên khách hàng mua*, **Hình thức thanh toán*** (`Tiền mặt`/`Công nợ`/`Chuyển khoản` — Công nợ create-only), Mã số thuế* + search-customer modal (auto-fills Tên đơn vị + Địa chỉ). Fieldset **Chi tiết**: VAT editable rate (default 10) → computed `Tổng thành tiền`/`Tiền thuế`/`Tổng thanh toán`. Grid **Hàng hóa đã thêm** (`STT | Hàng hóa | DVT | Số lượng | Đơn giá | Thành tiền | ##`). **Source-ticket import**: Loại phiếu thu (`Bán hàng`/`Phiếu sửa chữa`/`Nội dung khác`) + Số phiếu search → staging grid (`Loại phiếu | Số phiếu | Model | Mã hàng | Hàng hóa | DVT | Số lượng | Đơn giá | Trừ VAT | ##` with `[+]` add). Ghi chú textarea.

### Catalog (danh-muc, 14 pages)

- **C1 Model** (`ModelPage`) — columns exact: `## | [☑] | Tên model | Model Code | Nhà sản xuất | Sản phẩm | Người tạo | Ngày tạo | Ghi chú | Chọn`. Side form: Sản phẩm* autocomplete + `[+]`, Nhà sản xuất* autocomplete + `[+]`, Model Code (auto when blank), Tên model*, Ghi chú textarea. Filters: Sản phẩm autocomplete, NSX autocomplete, Tên Model. **Xuất ra Excel** + **Import Excel**. Remove invented Trạng thái; Model Code optional.
- **C2 Nhà kho** (`NhaKhoPage`) — columns exact: `[☑] | Tên nhà kho | Địa chỉ | Chi nhánh | Kho xác | Chọn`. Add **Kho xác** boolean column + form checkbox (drives Xác inventory). Form: Tên nhà kho*, Địa chỉ textarea, Chi nhánh autocomplete, Kho xác checkbox. Remove invented Mã kho/Trạng thái.
- **C3 Ngăn chứa** (`NganChuaPage`) — columns exact: `[☑] | Nhà kho | Tên ngăn chứa | Chọn`. Form: Nhà kho* select, Tên ngăn chứa*. Filter: Nhà kho autocomplete + keySearch. Remove invented Mã ngăn/Trạng thái.
- **C4 Nhóm hàng hóa** (`NhomHangHoaPage`) — columns exact: `[☑] | Mã nhóm hàng hóa | Tên nhóm hàng hóa | Chọn`. Form: Mã nhóm (optional), Tên nhóm*. Remove invented Trạng thái.
- **C5 Hàng hóa** (`HangHoaPage`) — columns exact (15): `[☑] | Hình | Mã hàng | Mã hàng phụ | Tên hàng | Tiếng Anh | Nhóm hàng hóa | Nhà sản xuất | Tên model | Model dùng chung | Đơn vị | Người tạo | Ngày tạo | Serial | Chọn`. Row actions: edit (→ full-page editor) + **In Barcode** (`/Print/PrintTemHangHoa8?id=` print window). Filters: Nhóm hàng hóa, NSX autocomplete, Mã hàng hóa, Tên hàng hóa, Model autocomplete. **Import Excel** + **Xuất ra Excel**. Replace invented giá/tồn/Trạng thái columns.
- **C5b Hàng hóa editor** (`/danh-muc/hang-hoa/tao-moi`, `.../:id/sua`) — full-page per verified `page-Product-Create`. Fieldset **Thông tin hàng hóa - linh kiện**: Nhóm hàng hóa* (8 types), Có Serial checkbox, Nhà sản xuất autocomplete + `[+]`, Model* autocomplete + `[+]` + `Dùng chung nhiều model` checkbox, Model dùng chung text, Đơn vị tính* (`Cái`/`Thanh`/`Bộ`/`Lít`/`Mét`/`Kg`/`g`), Phát sinh tự động checkbox, Mã hàng hóa*, Mã hàng hóa phụ, Tên hàng hóa*, Tên Tiếng Anh, Vị trí linh kiện textarea, **3 price tiers** `Giá mua`/`Giá bán sỉ`/`Giá bán lẻ` (money-masked). Fieldset **Model dùng chung** (when Multi): NSX+Model autocompletes + `Thêm model` → grid (`STT | Nhà sản xuất | Model | ##`). Fieldset **Chọn hình**: file upload + preview. Toolbar `Lưu` / `Lưu & Thêm mới`. Drop invented tồn-kho field.
- **C6 Nhà sản xuất** (`NhaSanXuatPage`) — columns exact: `[☑] | Mã nhà sản xuất | Tên nhà sản xuất | Ghi chú | Chọn`. Form: Mã NSX (optional), Tên NSX*, Ghi chú textarea. Remove invented Nước SX/Trạng thái.
- **C7 Sản phẩm** (`SanPhamPage`) — columns exact: `[☑] | Tên sản phẩm | Mã sản phẩm | Nhóm sản phẩm | Tiền khoán | Chọn`. Add **Tiền khoán** money column + form field. Form: Nhóm Sản Phẩm* autocomplete + `[+]`, Mã sản phẩm (optional), Tên sản phẩm*, Tiền Khoán money. Remove invented Nhà sản xuất requirement + Trạng thái.
- **C8 Khu vực** (`KhuVucPage`) — columns exact: `## | [☑] | Tên Tỉnh | Tên Quận | Tên Xã/Phường | Tên khu vực | Cây số | Tiền công | Tiền công 2 | Chọn`. Form: Tỉnh* autocomplete + `[+]`, Quận* autocomplete + `[+]`, Phường/Xã autocomplete + `[+]`, Tên khu vực*, Cây số money, Tiền công 1 money, Tiền công 2 money. Filters: Tên khu vực, Tỉnh autocomplete, Quận autocomplete. Replace flat tinhThanh model with Tỉnh→Quận→Xã hierarchy (P1).
- **C9 Phường/Xã** (`PhuongXaPage`) — columns exact: `## | [☑] | Tên Tỉnh | Tên Quận | Tên Xã/Phường | Cây số | Tiền công | Tuyến | Chọn`. Form: Tỉnh* autocomplete + `[+]`, Quận* autocomplete + `[+]`, Tên phường xã*, Khoảng cách money, Tiền công money, Tuyến autocomplete. Filters: Tỉnh, Quận, Tên phường xã, Tuyến autocompletes. Replace invented Loại/khuVucId.
- **C10 Thời hạn** (`ThoiHanPage`) — columns exact: `[☑] | Tên | Loại | Thời Gian Bảo Hành | Chọn`. Form: Tên*, **Loại Thời Gian*** radio (`Tháng`/`Năm`), Thời Gian* number. Replace soNgay-days model. Title `Thời Gian Bảo Hành`.
- **C11 Phí giao** (`PhiGiaoPage`) — columns exact: `[☑] | Sản phẩm | Tên phí | Số tiền | Loại phí | Ghi chú | Chọn`. Form: Sản phẩm select (product-status, ~160 options + "Không chọn"), Tên phí giao*, Số tiền* money, **Loại phí*** (`Cộng`/`Trừ`/`Công`), Ghi chú. Replace wrong Khu-vực association.
- **C12 Đơn Vị Tính** (`DonViTinhPage`) — columns exact: `[☑] | Tên Đơn Vị Tính | Chọn`. Form: Tên đơn vị tính* (single). Add bulk delete + Lưu & Thêm mới. Remove invented Mã ĐVT/Trạng thái.
- **C13 Nhóm Sản Phẩm** (`NhomSanPhamPage`) — columns exact: `[☑] | Tên Nhóm | Chọn`. Form: Tên Nhóm. Add bulk delete + Lưu & Thêm mới. Remove invented Mã nhóm SP/Trạng thái.
- **C14 Lỗi sửa chữa** (`LoiSuaChuaPage`) — columns exact: `[☑] | Chi Nhánh | Tên Nhóm Sản Phẩm | Tên Lỗi Sửa Chữa | Tiền Công | Tiền Công DV | Chọn`. Form: Chi nhánh select (3 branches), Nhóm Sản Phẩm* autocomplete + `[+]`, Tên Sửa Chữa textarea, Tiền công money, Tiền công DV money. Filters: Chi nhánh autocomplete, Tên lỗi, Nhóm Sản Phẩm autocomplete. Replace maLoi/tenLoi/moTa model with labor-price model (branch × product group × repair).

### Customer (danh-muc)

- **CU1 Khách Hàng** (`KhachHangPage`) — columns exact (15): `STT | [☑ Chọn tất cả] | Tên khách hàng | Điện thoại | Điện thoại 2 | Địa chỉ | Phường/Xã | Quận/Huyện | Tỉnh | Email | Loại | Đại lý/Trạm | Người tạo | Ngày tạo | Chọn`. Row action = **Chỉnh sửa** only (no delete). **Nhóm khách hàng** 9-value taxonomy (`Khách lẻ`, `Đối tác MB/Nhà CC`, `Đại lý chính`, `Trung tâm bảo hành`, `Đại lý/Cửa hàng`, `Nhân viên công ty`, `Thợ sửa chữa`, `Cộng tác viên`, `Nhà xe - Chuyển phát`) — Loại column + filter + form. Header buttons **Thêm Khách Hàng** + **Thêm Đại Lý** (2 create flows). Filters: Tỉnh autocomplete, Quận autocomplete, Nhóm khách hàng, Tên khách hàng, Số điện thoại, Email, Địa chỉ. **Xuất Excel File**. Add model fields: Điện thoại 2, Phường/Xã, Quận/Huyện, Tỉnh, Đại lý/Trạm, Người tạo. Bulk-select drives **bulk delete only** (open decision #4). Default sort newest-first. Remove invented Mã KH/Tổng phiếu/Trạng thái.

### HR (nhan-su, 10 pages)

- **H1 Ngân Hàng** (NEW, replaces stub) — columns: `[☑] | Mã Ngân Hàng | Tên Ngân Hàng | Địa chỉ | Chọn`. Form: Mã Ngân Hàng*, Tên Ngân Hàng*, Địa chỉ textarea. Route `/nhan-su/ngan-hang`.
- **H2 Phụ Cấp** (NEW) — columns: `[☑] | Tên phụ cấp | Loại phụ cấp | GiaTri | Chọn`. Form: Tên Phụ Cấp*, **Loại*** (`Ăn Chia`/`Tiền mặt`), Giá Trị* money. Route `/nhan-su/phu-cap`.
- **H3 Loại Phạt Thưởng** (NEW) — columns: `[☑] | Loại | Tên Loại | Chọn` (Loại renders `Thưởng`/`Phạt`). Form: **Loại*** radio (`Thưởng`/`Phạt`), Tên Loại*. Route `/nhan-su/loai-phat-thuong`.
- **H4 Ứng Lương** (NEW) — columns: `## | [☑] | Tên Nhân Viên | Tên Kỳ | Ngày Ứng | Số Tiền | Ghi chú | Chọn`. Form: Nhân Viên autocomplete + `[+]`, Tên Kỳ autocomplete + `[+]`, Ngày Ứng date, Số Tiền money, Ghi Chú. Filters: NV autocomplete, **Kỳ** select. Route `/nhan-su/ung-luong`.
- **H5 Phòng Ban** (`PhongBanPage`) — columns exact: `[☑] | Mã Phòng Ban | Tên Phòng Ban | Chọn`. Add bulk delete + Lưu & Thêm mới. Remove invented Trạng thái.
- **H6 Chức Vụ** (`ChucVuPage`) — columns exact: `[☑] | Mã Chức Vụ | Tên Chức Vụ | Chọn`. Search matches name OR code. Add bulk delete. Remove invented moTa/active.
- **H7 Nhân Viên** (`NhanVienPage`) — columns exact (11): `STT | [☑] | Hình | Mã NV | Tên NV | Phòng | Giới tính | Ngày sinh | Điện thoại | Khóa | Chọn`. Add **Khóa/Mở khóa** toggle column + action (`data-lock` red lock `Khóa` / green unlock `Mở khóa`). Add Hình (photo) + Giới tính to model. Sort active-first. Filter: Mã/tên nhân viên + Phòng ban. Row edit → full-page editor.
- **H7b NhanVien editor** (`/nhan-su/nhan-vien/tao-moi`, `.../:id/sua`) — full-page per verified `page-NhanVien-Create`, ~28 fields across 5 fieldsets + photo panel. **Thông tin cơ bản**: Mã NV*, Giới Tính* (`Nữ`/`Nam`), Họ Tên*, Ngày sinh*, Phone 1, Phone 2, Email, Thường trú. **Thông tin làm việc**: Phòng Ban*, Chức vụ*, Ngày Làm Việc*, Chi nhánh*, Lương cứng* money, Phí nhân công money (HeSo → feeds Công BH/SC), Hình Thức Thanh Toán* (`Tiền mặt`/`Chuyển khoản`), Tiền đóng bảo hiểm money, **Phụ cấp** multi-select (from H2 list). **Thông tin xác thực**: CMND, Ngày Cấp, Địa chỉ, Nơi Cấp. **Thông tin ngân hàng**: Số Tài Khoản, Mã Số Thuế, Ngân Hàng select (from H1). **Thông tin liên hệ**: Người Liên Hệ, Thông Tin Liên Hệ. Bottom: Ghi Chú. Photo upload + preview. Toolbar `Lưu` / `Lưu & Thêm mới`.
- **H8 Bảng lương** (`BangLuongPage`) — columns exact (17): `[☑] | STT | Kỳ | Tên NV | Phòng | Chức vụ | Lương cứng | Bảo Hiểm | Phụ cấp | Tăng ca - Nghỉ | Ứng lương | Thưởng - Phạt | Công BH | Công SC | Tổng lương | Thực lãnh | Chọn` + **totals second header row** (per-money-column sums). Row actions: **Tạo bảng lương** (`/BangLuong/Create?nvId&maKy` worksheet), per-row **Print**, per-row **Xuất excel** (conditional by chức vụ). Toolbar: **Xuất file excel** (bulk), **Cập nhật tiền công KV**, Tải lại trang. Filters: **Kỳ** select, Tên nhân viên, Phòng ban select. Aggregate `Tổng lương: {sum} VND`. Emit one row per employee × kỳ (empty money + `Tạo bảng lương` CTA when no record). Remove invented Trạng thái.
- **H9 Chấm công** (`ChamCongPage`) — columns exact (12): `[☑] | STT | Tên NV | Giới tính | Chức danh | Chi nhánh | Loại chấm | Chấm công | Ngày chấm công | Ngày tạo | Kỳ | Loại trừ | Chọn`. Re-model as exception-record CRUD (replace clock-in/out). Side form: Nhân viên* select, Ngày chấm date, Kỳ autocomplete, **Loại chấm công** (`Nghỉ`/`Nghỉ nữa ngày`/`Đi trễ`/`Tăng ca`/`Về sớm`) + conditional quantity field, **Loại trừ lương** (`Trừ tiền`/`Trừ ngày công`). Filters: keySearch + **Kỳ** select. Bulk delete.
- **H10 Chấm công tổng hợp** (`ChamCongTongHopPage`) — columns exact (10): `STT | Mã NV | Tên NV | Chi nhánh | Ngày chấm công | Ngày nghỉ | Giờ tăng ca | Số giờ trễ | Giờ về sớm | Xem`. Re-model as per-employee per-kỳ totals (replace day matrix). Filters: keySearch + **Kỳ** select. **Xuất Excel** + per-row **Xem** drill-down.

Non-functional: all lists mock-backed via TanStack Query; catalog side-forms are master-detail panels or Sheets (keep existing modernized Sheet pattern but add Lưu & Thêm mới); server-style pagination; deviations (persistent side-panel vs Sheet) accepted + documented.

## Architecture

- **CrudTablePage extension (P2 primitives)**: add opt-in bulk-select (checkbox col + `Chọn tất cả` + bulk-delete toolbar), `Lưu & Thêm mới` in CrudSheet, `ServerAutocomplete`-with-`[+]` as a `FormField` type, Excel export toolbar button. These are P2-owned; this phase consumes them via config flags (`config.bulkDelete`, `config.saveAndNew`, `config.export`, field `type: 'autocomplete-create'`).
- **Model changes** (`src/types/masterdata-types.ts`, `finance-types.ts`): extend/replace entities per verified schemas — KhuVuc **(keep the symbol name; re-model its fields to Tỉnh/Quận/Xã ids + Cây số/Tiền công/Tiền công 2, backed by P1's `TUYEN`/Tỉnh-Quận-Xã lookup — Finding 2, no second `KhuVuc`)**. **⚠ Red-team H3 — the KhuVuc re-model touches ~15 files, not 4:** grep `maKhuVuc`/`tenKhuVuc`/`KHU_VUC_ROWS`/`khuVucId`/`KhuVuc` before editing and update ALL consumers, explicitly including the **hardcoded search-field allowlist at `CrudTablePage.tsx:45`** (`'tenKhuVuc'`) and the `khuVucId` FK references in the customer + phi-giao mocks. List them (Contract-Verifier count) in the phase report; do NOT write "update all consumers." PhuongXa (Tỉnh/Quận + Khoảng cách/Tiền công/Tuyến-link), ThoiHan (Loại + value), PhiGiao (productStatusId/loaiPhi/ghiChu), LoiSuaChua (branchId/nhomSanPhamId/tienCong/tienCongDV), KhachHang (9-value type + location + phone2/daiLy/nguoiTao), ChungTu (12-type + collection state + source link), CongNo (per-ticket), HoaDon (MST/hinhThuc/nguoiLap + VAT rate + line items), NhanVien (~28 fields + gioiTinh/photo/locked + PhuCap link + HeSo), BangLuong (17-col — see Payroll note re Finding 12), ChamCong (exception records), Kỳ references throughout.
- **Live-layer data (D5, not seed regeneration)**: wire the P1 lookup modules into the LIVE `finance-mock.ts` + `masterdata/*` layers. **Customer rows already exist (red-team Failure-Mode Finding 6): `KHACH_HANG_ROWS`** in the masterdata layer (via `khach-hang.config.ts`) is the live customer store — this phase **re-models those existing rows** to the 9-value taxonomy + location/phone2/daiLy/nguoiTao fields; it does NOT create a new parallel customer store. (The dead `SEED_CUSTOMERS` P1 deleted was never this store; repair-create customers derive separately from `MOCK_TICKETS` — leave that path alone. Document the two customer surfaces in the phase report so they don't get "unified" by accident.) Add labor prices, tiền khoán, employee full profiles, payroll inputs, chấm công exceptions, ChungTu 12-type vouchers, per-ticket receivables to the live layers. This phase edits the layers pages read; it does not fork or regenerate `SEED_*`.
- **No wall-clock overdue (red-team M1):** the reference Công nợ has **no Hạn TT / due-date / overdue concept** (F2 spec removes the invented `hanTT`/`han_thanh_toan`). The current `finance-mock.ts:100` computes overdue via `new Date(hanTT) < new Date()` — **delete that path entirely** (not "replace with a clock"); Công nợ is per-ticket receivables (`Số tiền`/`Đã trả`/`Còn lại`), no time-relative state. This removes the last wall-clock determinism trap in finance.
- **Add the 3rd branch here (validation V1):** extend `BranchId`/`BRANCHES` from 2 to **3** — add `Cộng tác viên tuyến huyện` (the reference's ChungTu/LoiSuaChua branch select value; C14 form already assumes 3 branches). P1-P5 ran on 2; P6 owns the extension and must update every branch-typed consumer (branch filters, KPI per-branch counts, finance/inventory branch weights, `BRANCH_NAME`, any `BranchId` switch). List the touched files in the phase report (Contract-Verifier count). This is a deliberately-deferred ripple — do it as one coherent step early in P6 so the rest of the phase builds on 3.
- **New pages**: 4 HR CRUDs (`NganHangPage`, `PhuCapPage`, `LoaiPhatThuongPage`, `UngLuongPage`) as CrudTablePage configs; 3 full-page editors (Invoice composer via P2 template, Hàng hóa editor, NhanVien editor) as feature dirs.
- **Finance re-model**: `ThuChiPage`/`CongNoPage`/`HoaDonPage` rewired to reference column configs; ChungTu KPI boxes computed search-scoped; Công nợ settle-debt modal creates a thu voucher (mock mutation); Invoice composer imports source-ticket lines.
- **Payroll: static seeded rows, formula deferred (Finding 12)**: the exact payroll formula is unspecified (Unresolved #1) — do NOT build a speculative derivation engine for mock data. Seed `BangLuong` as **static rows per employee × Kỳ** (17 columns populated with plausible, internally-consistent values — the component columns `lương cứng`/`phụ cấp`/`bảo hiểm`/`tăng ca`/`nghỉ`/`ứng lương`/`thưởng-phạt`/`công BH-SC` set directly, `tổng`/`thực lãnh` = a documented simple sum of them so the row is arithmetically consistent). Un-created state = empty + `Tạo bảng lương` CTA. If a real formula is later specified, derivation replaces the static sum then — flagged in the phase report, not fabricated now.

## Related Code Files

- Create: `src/pages/nhan-su/{NganHangPage,PhuCapPage,LoaiPhatThuongPage,UngLuongPage}.tsx` + `src/config/crud-configs/{ngan-hang,phu-cap,loai-phat-thuong,ung-luong}.config.ts`
- Create: `src/features/invoice-composer/invoice-composer-page.tsx`, `src/features/product-editor/product-editor-page.tsx`, `src/features/employee-editor/employee-editor-page.tsx` (+ tests)
- Create: `src/features/finance/{lap-phieu-thu-modal,lap-phieu-chi-modal,thanh-toan-cong-no-modal}.tsx`, `src/features/customer/{them-khach-hang-modal,them-dai-ly-modal}.tsx`
- Create: config spec test files per page (column-header assertions)
- Modify: `src/pages/tai-chinh/{ThuChiPage,CongNoPage,HoaDonPage}.tsx`, `src/config/finance-tables/{thu-chi,cong-no,hoa-don}.config.ts`
- Modify: `src/pages/danh-muc/*` (14 catalog pages + KhachHangPage) + `src/config/crud-configs/*` (14 catalog + khach-hang)
- Modify: `src/pages/nhan-su/{PhongBanPage,ChucVuPage,NhanVienPage,BangLuongPage,ChamCongPage,ChamCongTongHopPage}.tsx` + configs
- Modify (LIVE layers, D5): `src/types/{masterdata-types,finance-types}.ts`, `src/mock/finance-mock.ts`, `src/mock/masterdata/*`, `src/routes/index.tsx` (4 HR pages + 3 editor routes, remove stubs), `src/config/nav-config.tsx` (labels). **Import (read-only)** the P1 lookup modules under `src/mock/seed/` — do not regenerate them.
- Delete: 4 `stub()` route entries (replaced by real pages)

## TDD Plan

1. **Characterization tests (lock survivors)**: each existing page renders rows + pagination from its current mock fn; CrudTablePage current single-save + per-row delete behavior; existing routes resolve. Lock `finance-mock` current exports before re-model.
2. **Failing spec tests (cite section files)** — column-header arrays + taxonomy option lists are primary assertions:
   - Finance: `thu-chi.config.test.ts` headers === F1 15-col list; Loại filter has 12 options; Tình trạng options === `['Chưa thu','Đã thu','Đã thu ngoài','Chưa chi','Đã chi']`; header buttons `Lập Phiếu Thu`+`Lập Phiếu Chi`; per-row action is print only. `cong-no.config.test.ts` headers === F2 10-col, NO checkbox col; `hoa-don.config.test.ts` headers === F3 10-col incl. `Mã Số Thuế`; invoice-composer VAT rate default 10 → derives Tiền thuế; source-import Loại options `['Bán hàng','Phiếu sửa chữa','Nội dung khác']`.
   - Catalog: each config test asserts exact header array (C1–C14 above); `nha-kho` has `Kho xác`; `san-pham` has `Tiền khoán`; `khu-vuc` has `['Tên Tỉnh','Tên Quận','Tên Xã/Phường','Tên khu vực','Cây số','Tiền công','Tiền công 2']`; `thoi-han` form has Loại radio `['Tháng','Năm']`; `phi-giao` Loại phí `['Cộng','Trừ','Công']`; `loi-sua-chua` has `['Chi Nhánh','Tên Nhóm Sản Phẩm','Tên Lỗi Sửa Chữa','Tiền Công','Tiền Công DV']`; product-editor has 3 price tiers `Giá mua`/`Giá bán sỉ`/`Giá bán lẻ`.
   - Customer: `khach-hang.config.test.ts` headers === CU1 15-col; Nhóm khách hàng filter has 9 options; header buttons `Thêm Khách Hàng`+`Thêm Đại Lý`; no Mã KH/Tổng phiếu.
   - HR: 4 new-page config tests (headers + form fields); `nhan-vien.config.test.ts` has `Khóa` column + Hình + Giới tính, active-first sort; employee-editor renders 5 fieldsets + ~28 fields + Phụ cấp multi-select; `bang-luong` headers === 17-col + totals row + emits empty-record `Tạo bảng lương` CTA; `cham-cong` Loại chấm công options === `['Nghỉ','Nghỉ nữa ngày','Đi trễ','Tăng ca','Về sớm']`, Loại trừ `['Trừ tiền','Trừ ngày công']`; `cham-cong-tong-hop` headers === 10-col totals.
   - CrudTablePage extension: bulk-select renders `Chọn tất cả` + bulk-delete removes checked; `Lưu & Thêm mới` saves + resets form; autocomplete-create field opens `[+]` modal.
   - Mutation tests: `thanhToanCongNo(ticketId, amount)` creates a thu voucher + reduces Còn lại; invoice source-import pushes lines; NhanVien lock toggles `data-lock`.
3. **Implementation order to green**: CrudTablePage extension (bulk/save-new/autocomplete-create/export) → type + live-layer model changes → catalog configs (C1–C14, mechanical) → customer config + 2 create modals → HR 4 new pages → HR existing pages + employee editor → finance 3 pages + 3 create flows + invoice composer → static payroll rows (Finding 12) → full suite + gates.

## Implementation Steps

1. **CrudTablePage + CrudSheet extension** (P2-owned, consumed here): wire `config.bulkDelete` (checkbox col + `Chọn tất cả` + red bulk-delete toolbar), `config.saveAndNew` (`Lưu & Thêm mới`), `config.export` (Xuất ra Excel via `export-xlsx`), FormField `type: 'autocomplete-create'` (ServerAutocomplete + `[+]` modal). Interface-check against P2 delivery first.
2. **Types + live-layer data** (`masterdata-types.ts`, `finance-types.ts`, `finance-mock.ts`, `masterdata/*`): apply all model changes listed in Architecture; edit the live mock rows to reference schemas, importing P1's taxonomy/Kỳ/Tỉnh-Quận-Xã/`TUYEN` lookups. Do not regenerate `SEED_*` (deleted in P1).
3. **Catalog configs C1–C14**: rewrite each `*.config.ts` to verified columns + form fields + filters + bulk-delete + save-new + export where specced; add autocomplete-create fields (Model, Sản phẩm, Khu vực, Phường/Xã, Lỗi SC). Wire Hàng hóa + full-page editor route.
4. **Customer CU1**: rewrite `khach-hang.config.ts` to 15-col + 9-value type + location filters + Xuất Excel + bulk-delete; build `them-khach-hang-modal` + `them-dai-ly-modal`; default sort newest-first.
5. **HR new pages H1–H4**: 4 CrudTablePage configs + pages; wire routes (remove stubs); H4 Ứng Lương uses NV + Kỳ autocomplete-create + Kỳ filter.
6. **HR existing H5–H10**: Phòng Ban/Chức Vụ (bulk + clean invented); NhanVien 11-col + Khóa toggle + full-page editor (`employee-editor-page` 5 fieldsets); Bảng lương 17-col + totals + row/toolbar actions + Kỳ filter + **static seeded rows (Finding 12 — no derivation engine, documented simple sum)**; Chấm công exception-record CRUD; CC Tổng Hợp per-kỳ totals + Xem.
7. **Finance F1–F3**: ThuChi 15-col + 12-type + collection-state + Lập Phiếu Thu/Chi modals + per-row print + KPI boxes + 2 exports + source links; Công nợ 10-col per-ticket + thanh-toán modal; Hóa đơn 10-col + MST/hinhThuc/nguoiLap + bulk delete + composer route.
8. **Invoice composer** (`invoice-composer-page`): full-page per F3b via P2 line-item template; VAT rate math; source-ticket import staging grid + `[+]`.
9. Run `npm run test` → green; `npm run type-check && npm run lint && npm run build`; grep removed invented fields (`maKH`, `tongPhieu`, catalog `trang_thai`/`active` switches, `nuocSX`, cong-no `hanTT`, `soNgay` on thoi-han) to confirm no dead refs.

## Success Criteria

- [ ] Every finance/catalog/customer/HR list renders its exact verified reference column set (spec tests green); invented columns (Trạng thái switches, Mã KH, Nước SX, prices on Hàng hóa, day-matrix Chấm công) removed.
- [ ] 4 HR stub routes replaced by working CRUD pages; 3 full-page editors (Invoice composer, Hàng hóa, NhanVien ~28 fields) create/update mock records with dual save.
- [ ] Data models corrected: 12-type ChungTu + collection state, per-ticket Công nợ + settle flow, VAT-rate invoice, Tỉnh→Quận→Xã Khu vực/Phường-Xã + Tuyến, product-linked Phí giao, labor-price Lỗi sửa chữa, 9-value Nhóm khách hàng, exception Chấm công, 17-col Bảng lương + totals + Tạo bảng lương CTA.
- [ ] NhanVien Khóa/Mở khóa toggle works; Customer Thêm Khách Hàng + Thêm Đại Lý both create; ChungTu Lập Phiếu Thu/Chi + per-row print work.
- [ ] Cross-cutting: bulk-select + `Chọn tất cả` + bulk-delete on every list the reference has it; `Lưu & Thêm mới` on catalog forms; autocomplete + `[+]` quick-create on related-entity fields; Excel export where specced.
- [ ] Kỳ selector present on Ứng lương, Bảng lương, Chấm công, CC Tổng Hợp (P1 entity).
- [ ] `npm run type-check && npm run lint && npm run test && npm run build` all clean.

## Risk Assessment

- **Largest breadth (28 pages)** → most are mechanical config rewrites once CrudTablePage extension + models land; batch by section (catalog first — most uniform, then customer/HR/finance). If timeboxed, ship column/model corrections + 4 new HR pages first (highest-severity), editors + static payroll rows second.
- **Model changes ripple to repair/inventory phases** → coordinate: KhachHang/NhanVien/Kỳ shapes are consumed by P3/P4/P5; keep additive where possible, run full suite after type changes. P1 owns the lookup modules — import, don't fork.
- **Payroll formula unspecified (Finding 12)** → do NOT build a derivation engine on a guessed formula. Seed static `BangLuong` rows with `tổng`/`thực lãnh` as a documented simple sum of the component columns; flag the deferred formula in the phase report. Replace with real derivation only if/when the formula is specified.
- **Mock-write durability (Finding 6)** → settle-debt vouchers, created customers/employees, quick-created lookups live in module memory only and are lost on any reload (see plan.md §D5 + P2 note). Do not claim persistence; tests cover create→refetch within one session.
- **Công nợ settle → thu voucher coupling** → settle mutation creates a ChungTu row; keep both in the same mock store so F1 reflects F2 payments.
- **Invented-field removal breaking deep links / saved state** → grep + fix all references; no localStorage schema migration needed (mock only).
- **Rollback**: changes confined to `src/pages/{tai-chinh,danh-muc,nhan-su}/**`, `src/config/{crud-configs,finance-tables}/**`, `src/features/{invoice-composer,product-editor,employee-editor,finance,customer}/**`, `src/types/*`, `src/mock/{finance-mock.ts,masterdata/*}` (live layers), routes/nav — single phase-commit revert restores Phase-5 state.

## Unresolved

1. **Bảng lương formula (drives Finding 12 decision)** — exact computation of Công BH/Công SC (technician piece pay from HeSo + Lỗi sửa chữa Tiền Công/Tiền Công DV × completed tickets in Kỳ) and Thực lãnh (Tổng lương − deductions) is not specified. **Resolution: seed static rows with `tổng`/`thực lãnh` as a documented simple sum of the component columns (no derivation engine on a guessed formula).** Confirm the real formula before any derivation is built; until then the static rows are the deliverable.
2. **Công nợ settle-debt modal fields** — congno.js not mirrored; assuming amount + payment-method + note → creates thu voucher. Confirm exact modal shape.
3. **ChungTu Lập Phiếu Thu / Lập Phiếu Chi form fields** — chungtu.js not mirrored; deriving fields from the 15 columns + 12 loại; confirm.
4. **Customer bulk-select action** — customer.js not mirrored; no bulk toolbar in HTML → defaulting to bulk delete (open decision #4). Confirm before shipping bulk UI.
5. **Phí giao Sản phẩm option source** — ~160 product-type options; assuming they come from the Sản phẩm (ProductStatus) list; confirm the option universe.
6. **Đại lý/Trạm parent linkage** — customer `Đại lý/Trạm` column references a parent dealer customer; assuming self-referential customer FK filtered to dealer types.
7. **Số Phiếu SC/NK source-doc modal** (ChungTu PNK detail) — receiving-note detail popup content unverified; reuse P5 voucher-detail-modal.
