---
phase: 5
title: "Warehouse + stock-out"
status: completed
effort: "XL"
---

> **Completed 2026-07-06.** Gate clean: `test` 247 pass (65 files, deterministic ×2), fresh `tsc --noEmit`, `lint` (0 errors), `build` all green. Built via 3 parallel agents over a shared `src/domains/warehouse/` foundation (types, Kỳ carry-forward inventory math per Finding 10 — opening chains across periods, closing=opening+nhập−xuất, negatives unclamped; list-data + fetchers + mutations duyetTraLK/traHang/thuXacLK/traLK). Agent A: 3 inventory views (W2 20-col / W3 19-col / W4 16-col) + KPI trio (KpiBox/KpiTrio, negatives shown) page-level composition (Finding 11, no new host). Agent B: warehouse lists W1 (Nhập Kho + editor), W5 (DSCapLK re-model → Danh sách sử dụng linh kiện, 21-col, status-badge, action-state cell), W6 (DSTraLK 18-col + Duyệt), W7 (NEW DSTraLKXac 20-col + Trả hàng). Agent C: stock-out S1-S4 lists + 5 line-item editors (Cấp LK, Bán Hàng create/edit, Trả Hàng, Chuyển Kho dual same/cross-branch). Routes + nav wired in main session. Invented models (refund fields, invented statuses, Trạng thái columns) grep-clean in rewritten pages; all prints via P2 openPrintWindow.

# Phase 5: Warehouse + stock-out

## Overview

Rebuild the 6 warehouse pages (`src/pages/quan-ly-kho/`) and 4 stock-out pages (`src/pages/xuat-kho/`) to reference parity, plus add the missing **Danh sách trả linh kiện xác** (`/CheckOutForTech/DSTraLKXac`) page. Replaces header-only CrudSheet create forms with the P2 full-page line-item editor template (6 editors), re-models the 3 inventory views to their distinct reference column sets with Từ Kỳ/Đến Kỳ period selects + KPI boxes + row drill-downs, corrects DSCapLK to an issued-parts usage list, and adds the Duyệt / Trả hãng bulk approval workflows. All data mutates the mock store (D4); every list gets its verified reference column set and Excel export.

## Context links

- Spec (contract): `plans/reports/ref-ui-parity-sections/section-warehouse.md` (7 pages) and `section-stock-out.md` (4 pages) — read BOTH fully incl. their `## Addendum — verified from mirrored partials (260703)` sections, which carry the **exact verified column lists** and the 6 full-page editor specs. The addenda override the pre-addendum "unverified" notes.
- Local code consumed: `src/pages/quan-ly-kho/*` (6 files), `src/pages/xuat-kho/*` (4 files), `src/config/finance-tables/{nhap-kho,ton-kho,thu-hoi-lk,ds-tra-lk,cap-linh-kien,ban-hang,tra-hang,chuyen-kho}.config.ts`, `src/components/finance/{inventory-kpi-strip,stock-filter-bar,period-range-picker}.tsx`, `src/components/crud/CrudTablePage.tsx`, `src/constants/routes.ts`, `src/config/nav-config.tsx`, `src/mock/seed/*`.
- P1 outputs assumed present (as importable lookup modules, D5 — not "regenerated seed"): Kỳ lookup (month periods, e.g. `10/2020`, `7/2026`), legacy 15-status module (DSCapLK Tình trạng phiếu filter + Số phiếu hãng badge), Tỉnh→Quận→Xã + `TUYEN` (not central here). **Data layer (D5):** warehouse/stock-out pages read the LIVE `src/mock/inventory-mock.ts` + finance-table configs; this phase edits those in place and adds the new `src/domains/warehouse/` domain (incl. the Finding-10 Kỳ transaction ledger). Product/serial/warehouse/cabinet/manufacturer/model data lives in the live `masterdata/*` + `reference-data.ts` layers — relate against those (P6 owns catalog re-model; P5 reads what it needs and, where a relational ledger is missing, builds it in the warehouse domain).
- P2 primitives consumed: full-page line-item editor template, DataTable bulk-select extension (checkbox + `Chọn tất cả` + toolbar slot), `export-xlsx.ts`, `print-window.tsx`, `ServerAutocomplete` (+ `[+]` quick-create), Kỳ picker.

## Requirements

Each requirement traces to a `section-warehouse.md` / `section-stock-out.md` gap row or verified column list. **Exact Vietnamese column headers are normative — build them verbatim, in order.**

### Warehouse (quan-ly-kho)

- **W1 Nhập Kho list** (`NhapKhoPage`) — columns exact: `[☑ Chọn tất cả] | Số phiếu | Số đặt hàng | Số hóa đơn | Nhà cung cấp | Hình thức thanh toán | Nhà kho | Số tiền | Người lập | Ngày lập | Ghi Chú | Chọn`. Per-row: **In** (`/Print/Receiving?id=` → print window), **Chi tiết** modal. Toolbar: **Thêm mới** (→ create editor), refresh (`Tải lại trang`), **Xuất ra Excel**; `Tổng tiền` info-box (blue) above table. Remove invented `Trạng thái` column/filter (reference has none). Ngày lập = `dd/MM/yyyy hh:mm AM/PM`. Số phiếu format `PNK-YYYYMMDD-n`.
- **W1b Nhập Kho create editor** (`/quan-ly-kho/nhap-kho/tao-moi`) — full-page per verified `page-Receiving-Create` spec. Toolbar `Lưu` / `Lưu & Thêm mới` / `In` / `Danh sách nhập kho`. Fieldset **Thông tin khách hàng**: Số phiếu (readonly "Phát sinh tự động"), Ngày nhập (disabled datepicker), Nhà kho*, Ngăn chứa* (cascade from kho), Hình thức thanh toán* (`Tiền mặt`/`Công nợ`/`Chuyển khoản`), Khoản chi (readonly "Chi mua hàng"), Nhóm khách hàng* (9 values: `Khách lẻ`, `Đối tác MB/Nhà CC`, `Đại lý chính`, `Trung tâm bảo hành`, `Đại lý/Cửa hàng`, `Nhân viên công ty`, `Thợ sửa chữa`, `Cộng tác viên`, `Nhà xe - Chuyển phát`), Nhà cung cấp* (ServerAutocomplete + `[+] Thêm mới nhà cung cấp`), Số hóa đơn, Người giao, Ngày nhập hóa đơn, Ngày giao, Số đặt hàng (textarea), Ghi chú (textarea). Fieldset **Chi tiết** (line entry): NSX autocomplete, Model autocomplete + `[+] Thêm mới model`, Hàng hóa* autocomplete + `[+] Thêm mới hàng hóa`, Số lượng, Giá mua mới + `Cập nhật giá` checkbox, **Thêm hàng**; serial textarea when product IsSerial. Line grid `Danh sách hàng nhập`: `# | Mã | Tên | Ngăn chứa | Số lượng | Đơn giá | Thành tiền | Cập nhật giá | Serial | ##`. Validation: kho/ngăn/hình thức required; nhà cung cấp min 1 (`Vui lòng chọn nhà cung cấp!`); ≥1 line (`Vui lòng thêm hàng hóa!`).
- **W2 Xem Tồn Kho** (`XemTonKhoPage`) — columns exact (20): `STT | ## | Chi nhánh | Mã hàng | Tên hàng | Nhóm hàng | Model | Giá vốn đầu kỳ | Tồn đầu kỳ | Nhập trong kỳ | Xuất trong kỳ | Tồn | Giá vốn trong kỳ | Tồn cuối kỳ | Tổng tiền | Nhà sản xuất | Nhà kho | Ngăn chứa | Kỳ | Có serial`. Filters: Chi nhánh, Nhà kho, Ngăn chứa (cascade), Nhóm hàng hóa (8 real product types: `Điện lạnh`, `Điện tử`, `Điện Thoại`, `Điện gia dụng`, `linh kiện điện tử`, `Dụng cụ sửa chửa`, `Nguyên vật liêu sửa chửa`, `Nhà vệ sinh`), NSX autocomplete, Model autocomplete, Mã/tên hàng hóa, **Từ Kỳ / Đến Kỳ** (Kỳ picker, replaces PeriodRangePicker). KPI boxes: `Tồn đầu kỳ` (green) / `Tổng tiền` (blue) / `Tổng tồn` (yellow). Row actions: **Cập nhật** (warehouse-location modal), **Xem chi tiết** (detail page/modal), NK/XK cells are drill-down links. Rows-per-page 20/30/50/100/150/200/300. Read-only view — drop generic create/delete Sheet.
- **W3 Xem Tồn Kho LK Xác** (`TonKhoLKXacPage`) — columns exact (19, same as W2 **without** `Tổng tiền`). Kho filter restricted to carcass warehouses ("Kho LK Xác Daklak"). Row actions: **Cập nhật** + **Xem chi tiết** (→ `/ViewInventory/ViewDetail_Xac` equiv detail) + NK/XK drill-downs. KPI boxes (same trio). Title `Xem Tồn Kho Linh Kiện Xác` (fix "xác nhận" misnomer). KPI `Tổng tồn` may be negative — do not clamp.
- **W4 Xem Tồn Kho Kỹ Thuật** (`TonKhoKyThuatPage`) — columns exact (16): `STT | ## | Chi nhánh | Kỳ | Kỹ thuật | Mã hàng | Tên hàng | Nhóm hàng | Nhà sản xuất | Model | Tồn đầu kỳ | Nhập trong kỳ | Xuất trong kỳ | Tồn | Giá vốn trong kỳ | Tồn cuối kỳ`. Filter adds **Nhập tên kỹ thuật** autocomplete (technician axis — the page's defining dimension); Từ Kỳ/Đến Kỳ; NSX/Model autocompletes; Nhóm hàng hóa. KPI trio. Row action: **Trả linh kiện kho kỹ thuật** modal (return-part-from-technician). Drop Kho/ĐVT columns.
- **W5 DSCapLK** (`ThuHoiLKPage` → re-modeled as **Danh sách sử dụng linh kiện**) — columns exact (21): `## | Tình trạng | Số phiếu cấp | Số phiếu SC | Số phiếu hãng | Model | Serial | NSX | Nhà kho | Mã hàng | Tên hàng | Kĩ thuật | Mục đích | Ngày cấp | Người cấp | Ngày giao | Ngày TX | Người TX | Số lượng cấp | SL Trả | Chọn`. `Tình trạng` cell = state-dependent action buttons: **Thu xác LK** (`.ma-modal-thuxac-d`) + **Trả Linh kiện** (`.ma-modal-tralk-d`); when returned → **In Tem Trả Xác** (`/Print/PrintTemDanXac?id=`) + `Đã trả xác` label. `Số phiếu hãng` embeds color-coded ticket-status badge (legacy 15 hex). Filters (11): Chi nhánh, Nhà kho (cascade), Kỹ thuật autocomplete, Số phiếu cấp, **Tình trạng phiếu** (15 legacy statuses), Số phiếu SC/hãng, Mã sản phẩm autocomplete, **Mục Đích** (`Sữa chữa dịch vụ`/`Bảo hành`/`Kỹ thuật mượn`), **Tình trạng** (`Đã trả xác LK`/`Chưa trả xác LK`/`Có trả LK`/`Chưa trả LK`), NSX autocomplete, DateType (`Ngày Cấp`/`Ngày Giao Phiếu`) + date range. KPI boxes: `Tổng cấp` (yellow) / `Tổng tiền LK chưa giao` (blue) / `Tổng tiền LK đã giao` (green). Row action **Chi tiết**. List-only (drop create/edit/delete). `SL Trả` text-danger.
- **W6 DSTraLK** (`DsTraLKPage`) — columns exact (18): `## | [☑ Chọn tất cả] | Tình trạng | Hình thức | Mã hàng | Tên hàng | Kĩ thuật | SL | Số phiếu cấp | Số phiếu SC | Số phiếu hãng | Model | Serial | NSX | Ngày tạo | Người tạo | Ngày duyệt | Người duyệt`. Row checkboxes drive **Duyệt** (bulk approve: `Chờ duyệt` → `Đã duyệt`) + per-row **Duyệt** confirm (`Bạn có chắn chắn Duyệt trả linh kiện?` → mock `DuyetTraLK` → toast `Duyệt thành công!`) + **In Phiếu Trả** (`/Print/PrintLKTraKT?id=csv`, alert `Vui lòng chọn phiếu để in` when none). Filters: Chi nhánh, Kỹ thuật autocomplete, Số phiếu cấp, NSX autocomplete, Số phiếu SC/hãng, Mã sản phẩm autocomplete, **Tình trạng** (`Chờ duyệt`/`Đã duyệt`), **Loại trả** (`Trả từ phiếu`/`Trả từ kỹ thuật`), date range. KPI box `Tổng số LK` (green). **Xuất ra Excel**. Remove invented status set (Chờ xác nhận/Đã trả/Hủy) + Lý do column. List + approve/print only.
- **W7 DSTraLKXac** (NEW page `/quan-ly-kho/ds-tra-lk-xac`) — columns exact (20): `## | [☑ Chọn tất cả] | Tình trạng | Mã vận đơn | Số phiếu cấp | Số phiếu SC | Số phiếu hãng | Model | Serial | Nhà kho | NSX | Mã hàng | Tên hàng | Kĩ thuật | Mục đích | Ngày TX | Người TX | SL | Ngày tạo | Người tạo`. `Tình trạng` = `Chưa trả hãng`/`Đã trả hãng` (label-warning/success). **Trả hàng** bulk (checked → modal `Trả linh kiện` mock `UpdateTraHang`, alert `Vui lòng chọn phiếu để trả` when none). Prints: **In BB Kỹ Thuật** (`/Print/PrintLKXacKT?id=csv`), **In Phiếu Trả Hãng** (`/Print/PrintLKXacHang?id=csv`) — alert `Vui lòng chọn phiếu để in` when none. Filters: Chi nhánh, Nhà kho (cascade), Kỹ thuật autocomplete, Số phiếu cấp, **Mã vận đơn**, Số phiếu SC/hãng, Tên khách hàng, Mã sản phẩm autocomplete, **Tình trạng** (`Chưa trả hãng`/`Đã trả hãng`), NSX autocomplete, date range. **Xuất ra Excel**. KPI box `Tổng số LK` (green). Add nav child under Quản Lý Kho + route.

### Stock-out (xuat-kho)

- **S1 Cấp Linh Kiện list** (`CapLinhKienPage`) — columns exact: `## | Số phiếu cấp | Ngày lập | Kỹ thuật | Số tiền | Người lập | Ghi chú | Chọn`. Toolbar: **Thêm phiếu cấp** (→ create editor). Aggregate `Tổng tiền` info-box (yellow). Per-row **Chi tiết** modal + **print** (`/Print/CapLKKyThuat?id=` → print window). NO checkbox column, NO per-row edit. Filters: Chi nhánh (cascade Nhà kho), Nhà kho, Kỹ thuật autocomplete, Số phiếu cấp, Số phiếu SC, Mã sản phẩm autocomplete, **Mục Đích** (`Sữa chữa dịch vụ`/`Bảo hành`/`Kỹ thuật mượn`), NSX autocomplete, date range. Buttons: **Tìm kiếm**, **Tìm chi tiết**, **Xuất ra Excel**, **Báo cáo lợi nhuận**. Số phiếu cấp `PCH-YYYYMMDD-n`; Ngày lập datetime.
- **S1b Cấp LK create editor** (`/xuat-kho/cap-linh-kien/tao-moi`) — per verified `page-CheckOutForTech-Create`. Toolbar `Lưu` / `Lưu & Thêm mới` / `Danh sách phiếu`. Fieldset **Thông tin chung**: Số phiếu (auto), Ngày cấp (disabled), Kỹ thuật* (autocomplete), Ghi chú. Fieldset **Chi tiết**: Phiếu sửa chữa* (autocomplete "Nhập vào Số Phiếu SC/ Số Phiếu Hãng / Tên Khách Hàng / Số Serial" → info panel Khách hàng/Phone/Ngày nhận/NSX/Model/Serial), Mục đích* select, Hàng hóa* autocomplete + price radios `Giá bán`(checked)/`Giá Sỉ`/`Giá Mua` + `Theo Serial` checkbox + "Giá:" addon, Số lượng* + "<= {tồn}" addon, **Thêm hàng**. Line grid `Danh sách hàng nhập` (14 cols): `# | Hình | Serial | Số phiếu | Mã hàng | Tên hàng | Nhà sản xuất | Model | Nhà kho | Ngăn chứa | Giá | Số lượng | Thành tiền | ##`, footer `Tổng tiền`. Validation: Kỹ thuật (`Vui lòng chọn kỹ thuật!`), ≥1 line (`Vui lòng chọn sản phẩm cấp cho kỹ thuật!`).
- **S2 Bán Hàng list** (`BanHangPage`) — columns exact: `[☑ Chọn tất cả] | Số phiếu | Ngày lập | Khách hàng | Điện thoại | Tổng tiền | Người lập | Ghi chú | Chọn`. Per-row: **Thêm hình** (image-upload modal), **Chỉnh sửa** (→ edit editor), **Xuất kho** (`/Print/Selling?id=` print window), **Chi tiết** modal. Bulk toolbar (refresh/edit/delete on checked). Filters: Chi nhánh (cascade), Nhà kho, Hình thức thu chi, Số phiếu/Ghi chú, Tên khách hàng, Mã hàng/Tên hàng, date range. Buttons: Tìm kiếm, Tìm chi tiết, Xuất ra Excel, Báo cáo lợi nhuận. Remove invented `Trạng thái` column. Số phiếu `PBH-YYYYMMDD-n`; Ngày lập datetime.
- **S2b Bán Hàng create/edit editor** (`/xuat-kho/ban-hang/tao-moi`, `.../:id/sua`) — per `page-SellingProduct-Create`. Toolbar `Lưu` / `Lưu & Thêm mới` / `In Phiếu BH` (`/Print/PrintSellingProduct?id=`) / `In Phiếu Thu` (`/Print/PrintSellingProductReceipt?id=`) / `Danh sách đơn hàng`. Fieldset **Thông tin khách hàng**: Số phiếu (auto), Ngày bán, Hình thức thanh toán* (`Tiền mặt`/`Công nợ`/`Chuyển khoản`), Khoản thu (readonly "Thu bán hàng"), Khách hàng* autocomplete + `[+]` quick-create, Ghi chú. Fieldset **Chi tiết**: Hàng hóa autocomplete + `Theo Serial` + price radios `Giá Lẻ`(checked)/`Giá Sỉ` + `Cập nhật giá` checkbox + PriceNew money + Số lượng + "<= {tồn}" + **Thêm hàng**. Line grid `Danh sách hàng`: `# | Hình | Serial | Tên | Model | Cập nhật giá | Giá | Số lượng | Thành tiền | ##`, footer `Tổng tiền:`.
- **S3 Trả Hàng list** (`TraHangPage`) — columns exact: `[☑ Chọn tất cả] | Số phiếu | Ngày trả | Hình thức trả | Người lập | Chọn`. Per-row: **Chỉnh sửa** (→ edit editor), **print** (`/Print/ReturnProduct_TraNCC?id=`), **Chi tiết** modal. Bulk toolbar. Filters: Chi nhánh, **Hình thức trả** (`Trả hàng từ kỹ thuật`/`Trả hàng từ khách hàng`/`Trả hàng cho nhà cung cấp`/`Trả hàng từ kho`), Số phiếu, date range. Buttons: Tìm kiếm, Xuất ra Excel, Xuất Excel Chi Tiết. **Replace invented customer-refund model** (khach_hang_ten/ly_do/tong_tien_hoan) with reference return-slip model. Số phiếu `PTH-YYYYMMDD-n`; Ngày trả datetime.
- **S3b Trả Hàng create/edit editor** (`/xuat-kho/tra-hang/tao-moi`) — per `page-ReturnProduct-Create`. **Hình thức trả** select reshapes form (option 4 label = `Trả xác linh kiện` on the editor); type block: Kỳ* + mã-phiếu autocomplete → **Thông tin phiếu** panel + source pick-table (`# | Hình | Mã phieu | Serial | Mã hàng | Tên hàng | Giá | Số lượng | Đã Trả | Số lượng trả | Thành tiền | ##` w/ per-row `Trả hàng`); alt block: Nhà khách hàng/Sản phẩm autocompletes + Kho/Ngăn chứa selects + Giá/Số lượng + `Thêm`. Return grid `Danh sách hàng nhập` (13 cols): `# | Hình | Mã phiếu | Số phiếu SC | Serial | Tên hàng | Kho | Ngăn chứa | Giá | Số lượng | Số lượng trả | Thành tiền | ##`. Toolbar `Lưu`/`Lưu & Thêm mới`/`In`/`Danh sách đơn hàng`.
- **S4 Chuyển Kho list** (`ChuyenKhoPage`) — columns exact: `[☑ Chọn tất cả] | Trạng thái | Số phiếu | Ngày lập | Từ chi nhánh | Từ kho | Đến chi nhánh | Đến kho | Loại | Người chuyển | Chọn`. TWO create buttons: **Chuyển cùng chi nhánh** (→ `/xuat-kho/chuyen-kho/cung-chi-nhanh`), **Chuyển khác chi nhánh** (→ `/xuat-kho/chuyen-kho/khac-chi-nhanh`). Bulk toolbar. Filters: Từ chi nhánh, Đến chi nhánh, Số phiếu, date range, **Trạng thái** (`Chưa xác nhận`/`Đã xác nhận`/`Không xác nhận`). Buttons: Tìm kiếm, Xuất Excel, Xuất Excel Chi Tiết. Replace invented statuses (Chờ xác nhận/Hoàn thành/Hủy).
- **S4b Chuyển Kho dual editors** — **cross-branch** (`page-MovingInventory-Create`): Từ chi nhánh* (own) / Từ nhà kho* (cascade) / Đến chi nhánh* (other branches) / Đến nhà kho* (cascade); line grid `Danh sách hàng chuyển`: `# | Hình | Serial | Mã | Tên | Số lượng | Số lượng chuyển | Giá | Thành tiền | ##`. **same-branch** (`page-MovingInventory-CreateBranch`): both branches locked to own; adds **Đến ngăn chứa*** (cabinet cascade) + per-line **Ngăn chứa** column: `# | Hình | Serial | Mã | Tên | Ngăn chứa | Số lượng | Giá | Thành tiền | ##`. Toolbar `Lưu`/`Lưu & Thêm mới`/`In` (`/Print/PrintMovingProduct?id=`)/`Danh sách chuyển kho`.

Non-functional: all lists AJAX-style via mock fns + TanStack Query; server-style pagination header `Trang X / Y`; horizontal-scroll containers (inventory 1200–1500px); create editors reachable as SPA routes (D1), never popup windows; `Số phiếu` always auto (`Phát sinh tự động`); dual-save on every editor. Deviations (accepted, document in report): "Tìm kiếm" vs "Tìm chi tiết" produce the same result set locally unless a distinct detail shape is trivially derivable; single bottom pager.

## Architecture

- **Line-item editor (Finding 11 — reuse P2's `LineItemEditor`, don't wrap it in a new page template)**: the 6 concrete editors are page components under `src/features/warehouse-editors/` (receiving, cap-linh-kien) and `src/features/stockout-editors/` (ban-hang, tra-hang, chuyen-kho-cung, chuyen-kho-khac), each composing the P2 `LineItemEditor` directly with its header-fields slot + line-column config. **Do NOT introduce a `LineItemEditorPage` super-template** that adds a config layer over P2's component — the editors differ enough (Trả Hàng's two-stage flow is a genuine outlier) that a shared page host would leak config for one-offs. Each editor serializes lines into a mock create/update mutation.
- **Inventory views (Finding 11 + red-team H5 — page-level composition, NOT a new host and NOT a CrudTablePage-header hack)**: W2/W3/W4 already render via `<CrudTablePage config={tonKhoConfig} />` for the **table body** (verified `XemTonKhoPage.tsx:59`), but the KPI strip + StockFilterBar already render **outside** it, at page level (`XemTonKhoPage.tsx:52-58`, above line 59). And the current `CrudConfig` contract **cannot** express what these pages need: `FilterConfig.type` is only `'text' | 'select' | 'date-range'` (`crud-types.ts:46`) — no KPI slot, no cascade filters, no ServerAutocomplete, no Từ Kỳ/Đến Kỳ picker. **So the correct pattern is the one the codebase already uses for exactly this case:** compose at the **page level** — `<InventoryKpiStrip/>` + the Kỳ/cascade filter bar rendered by the page, with `CrudTablePage config={…}` (or a page-level `DataTable`) for the grid. Precedent: `BangLuongPage.tsx:1` documents "Aggregate view: cannot be expressed by CrudTablePage" and hand-rolls a page-level DataTable — follow that. **Do NOT build `src/features/inventory/inventory-view.tsx`** (the over-abstraction the red team cut) **and do NOT bloat `CrudConfig`/`CrudTablePage` with a KPI-header + custom-filter system** (that just recreates the host under a different name across the 34 configs / 36 pages that share the contract). Supply three `CrudConfig`s for the grids (`ton-kho`, `ton-kho-lk-xac`, `ton-kho-ky-thuat`) + page-level KPI/filter composition per page.
- **Mock domain**: new `src/domains/warehouse/` — `types.ts` (ReceivingVoucher, CheckOutSlip, SellingOrder, ReturnSlip, MovingSlip, InventoryRow, IssuedPartUsage, PartReturn, PartReturnXac + line types), `mock-data.ts` (list fetchers with verified columns), `mock-mutations.ts` (create/update vouchers; `duyetTraLK(ids)`, `traHang(ids, maVanDon)`, `thuXacLK(id)`, `traLK(id)`). **Data layer (D5):** the existing inventory/finance-table pages read `src/mock/inventory-mock.ts` (+ its `*.config.ts`); this phase edits those live layers in place (verified/reference columns) and adds the new warehouse domain for editors/mutations. No `SEED_*` array is involved.
- **Inventory Kỳ math (Finding 10 — derive it, but keep it simple; red-team simplification):** today `inventory-mock.ts` fakes `ton_dau_ky` with `rngTK.int(10,200)` (verified `inventory-mock.ts:59`) — opening/closing are unrelated random numbers, so "Tồn đầu/trong/cuối kỳ per Kỳ" is not real. Replace the fabrication with a **deterministic Kỳ-indexed carry-forward** keyed on P1's Kỳ lookup: for each product×kho, seeded per-Kỳ (nhập, xuất) deltas with `tonDauKy(ky) = tonDauKy(prevKy) + nhập(prevKy) − xuất(prevKy)` and `tonCuoiKy = tonDauKy + nhập − xuất`. **This does NOT need a materialized transaction-row ledger** — a deterministic carry-forward function over the seeded per-Kỳ deltas satisfies every asserted invariant (opening chains across Kỳ, closing = opening+nhập−xuất, negative Tổng tồn possible) for ~30 read-only mock rows. Add individual transaction rows only if a later drill-down needs them. P5-owned (plan.md D5 assigns Finding 10 to P5).
- **DSCapLK re-model**: `ThuHoiLKPage` becomes `DanhSachSuDungLinhKienPage` (rename file + route label `Danh sách sử dụng linh kiện`; keep route path `thu-hoi-lk` to avoid churn, update nav label + breadcrumb). Its `Tình trạng` cell renders action buttons via a state machine (issued → Thu xác/Trả LK; returned → In Tem + label).
- **Bulk approve/return**: DSTraLK + DSTraLKXac use P2 bulk-select; toolbar buttons call mock mutations then invalidate. Confirm dialogs use existing alert-dialog primitive with exact VI strings.
- **Prints**: warehouse/stock-out print docs via P2 `print-window.tsx` (Receiving, CapLKKyThuat, Selling, PrintSellingProductReceipt, ReturnProduct_TraNCC, PrintMovingProduct, PrintTemDanXac, PrintLKTraKT, PrintLKXacKT, PrintLKXacHang) — thin document components under each feature's `prints/`.
- **KPI**: extend `inventory-kpi-strip.tsx` in place to accept the `{tonDauKy, tongTien, tongTon}` trio + not clamp negatives; reuse for DSCapLK (`Tổng cấp`/`chưa giao`/`đã giao`) and single-box variants (`Tổng số LK`, `Tổng tiền`) via a generic `KpiBox`.
- **Filters**: retire `stock-filter-bar.tsx`/`period-range-picker.tsx` generic combos where they diverge; build per-page filter configs using `ServerAutocomplete` + Kỳ picker (Từ Kỳ/Đến Kỳ). Cascade Chi nhánh→Nhà kho→Ngăn chứa via mock lookups.

## Related Code Files

- Create: `src/config/finance-tables/{ton-kho-lk-xac,ton-kho-ky-thuat}.config.ts` (two new `CrudConfig`s alongside the existing `ton-kho.config.ts`; W2/W3/W4 all render via `CrudTablePage config={…}` — NO new `inventory-view.tsx` host, Finding 11)
- Create: Kỳ-indexed carry-forward inventory math in `src/domains/warehouse/mock-data.ts` (seeded per-Kỳ nhập/xuất deltas per product×kho — Finding 10; opening chains across Kỳ, closing derived, not `rngTK.int`; no materialized transaction-row ledger unless a drill-down needs it)
- Create: `src/features/warehouse-editors/receiving-editor.tsx` (+ test), `src/features/stockout-editors/{cap-linh-kien-editor,ban-hang-editor,tra-hang-editor,chuyen-kho-cung-editor,chuyen-kho-khac-editor}.tsx`
- Create: `src/domains/warehouse/{types.ts,mock-data.ts,mock-mutations.ts}` (+ `mock-mutations.test.ts`, `mock-data.test.ts`)
- Create: `src/pages/quan-ly-kho/DsTraLKXacPage.tsx` (NEW W7) + `src/config/finance-tables/ds-tra-lk-xac.config.ts`
- Create: warehouse/stock-out `prints/*.tsx` (10 print docs) co-located under features
- Create: `src/features/inventory/tra-linh-kien-tech-modal.tsx`, `src/features/warehouse/{thu-xac-modal,tra-lk-modal,voucher-detail-modal}.tsx`
- Create per-page config test files asserting exact column headers (see TDD Plan)
- Modify: `src/pages/quan-ly-kho/{NhapKhoPage,XemTonKhoPage,TonKhoLKXacPage,TonKhoKyThuatPage,ThuHoiLKPage→DanhSachSuDungLinhKienPage,DsTraLKPage}.tsx`
- Modify: `src/pages/xuat-kho/{CapLinhKienPage,BanHangPage,TraHangPage,ChuyenKhoPage}.tsx`
- Modify: `src/config/finance-tables/{nhap-kho,ton-kho,thu-hoi-lk,ds-tra-lk,cap-linh-kien,ban-hang,tra-hang,chuyen-kho}.config.ts` (verified column sets)
- Modify: `src/components/finance/inventory-kpi-strip.tsx` (trio + negatives; generic KpiBox)
- Modify: `src/constants/routes.ts` (add DSTraLKXac + 6 editor routes + chuyen-kho dual create + ban-hang edit), `src/config/nav-config.tsx` (add `Danh sách trả LK xác` child; rename DSCapLK label), `src/routes/index.tsx` (wire routes)
- Delete: none (rename ThuHoiLKPage file only; retire generic StockFilterBar usage where superseded — leave file if reused elsewhere)

## TDD Plan

1. **Characterization tests (lock survivors)**: for each of the 10 existing pages, snapshot that it renders rows from its current mock fn + pagination works (`*Page.test.tsx` minimal). Lock `inventory-kpi-strip` current render before extending. Lock `routes.ts` existing inventory/stock-out paths resolve.
2. **Failing spec tests (cite section files)** — column-header arrays are the primary assertions:
   - `nhap-kho.config.test.ts`: headers === `['Số phiếu','Số đặt hàng','Số hóa đơn','Nhà cung cấp','Hình thức thanh toán','Nhà kho','Số tiền','Người lập','Ngày lập','Ghi Chú']` (+ checkbox + Chọn cols); NO `Trạng thái`.
   - Inventory config tests (rendered through `CrudTablePage`, Finding 11 — no `inventory-view`): `ton-kho` headers === the 20-col W2 list; `ton-kho-lk-xac` === 19-col (no `Tổng tiền`); `ton-kho-ky-thuat` === 16-col W4 list incl. `Kỹ thuật`; KPI strip renders `Tồn đầu kỳ`/`Tổng tiền`/`Tổng tồn`; negative `Tổng tồn` renders with minus sign; `Từ Kỳ`/`Đến Kỳ` pickers present (not date range). **Carry-forward test (Finding 10):** `tonDauKy(ky)` equals `tonDauKy(prevKy) + nhập(prevKy) − xuất(prevKy)` (assert the chain, derived not random) and `tonCuoiKy === tonDauKy + nhập − xuất` for the period; a stock-out-heavy fixture yields a negative `Tổng tồn` (no clamp).
   - `ThuHoiLKPage` (DSCapLK) test: 21-col header verbatim; `Tình trạng` cell renders `Thu xác LK` + `Trả Linh kiện` buttons for issued state, `In Tem Trả Xác` + `Đã trả xác` for returned; `Số phiếu hãng` badge uses legacy status hex; `Tình trạng phiếu` filter offers 15 statuses; `Mục Đích` filter options === `['Sữa chữa dịch vụ','Bảo hành','Kỹ thuật mượn']`; `Tình trạng` filter options === `['Đã trả xác LK','Chưa trả xác LK','Có trả LK','Chưa trả LK']`.
   - `DsTraLKPage` test: 18-col header; row Duyệt confirm text `Bạn có chắn chắn Duyệt trả linh kiện?`; success toast `Duyệt thành công!`; In Phiếu Trả empty-selection alert `Vui lòng chọn phiếu để in`; Tình trạng options `['Chờ duyệt','Đã duyệt']`; Loại trả options `['Trả từ phiếu','Trả từ kỹ thuật']`.
   - `DsTraLKXacPage` test: page mounts at route; 20-col header incl. `Mã vận đơn`; Trả hàng empty-selection alert `Vui lòng chọn phiếu để trả`; status labels `Chưa trả hãng`/`Đã trả hãng`; In BB Kỹ Thuật + In Phiếu Trả Hãng buttons present.
   - Stock-out list tests: `cap-linh-kien` headers === `['Số phiếu cấp','Ngày lập','Kỹ thuật','Số tiền','Người lập','Ghi chú']`, no checkbox; `ban-hang` === `['Số phiếu','Ngày lập','Khách hàng','Điện thoại','Tổng tiền','Người lập','Ghi chú']` (+ checkbox), no `Trạng thái`; `tra-hang` === `['Số phiếu','Ngày trả','Hình thức trả','Người lập']`; `chuyen-kho` === `['Trạng thái','Số phiếu','Ngày lập','Từ chi nhánh','Từ kho','Đến chi nhánh','Đến kho','Loại','Người chuyển']`.
   - Editor tests: `receiving-editor` renders `Lưu`/`Lưu & Thêm mới`/`In`, `Nhóm khách hàng` 9 options, line grid headers `['Mã','Tên','Ngăn chứa','Số lượng','Đơn giá','Thành tiền','Cập nhật giá','Serial']`, validation `Vui lòng chọn nhà cung cấp!` + `Vui lòng thêm hàng hóa!`; `cap-linh-kien-editor` validation `Vui lòng chọn kỹ thuật!` + `Vui lòng chọn sản phẩm cấp cho kỹ thuật!`; `tra-hang-editor` Hình thức trả option 4 === `Trả xác linh kiện`; `chuyen-kho-cung-editor` has `Đến ngăn chứa` + per-line `Ngăn chứa` col while `chuyen-kho-khac-editor` has `Số lượng chuyển` col.
   - `mock-mutations.test.ts`: `duyetTraLK` flips `Chờ duyệt`→`Đã duyệt`; `traHang(ids, maVanDon)` sets `Đã trả hãng` + stores vận đơn; create voucher mutations append to store + list refetch reflects it.
3. **Implementation order to green**: warehouse domain types/mock-data (incl. Kỳ ledger)/mutations → 3 inventory configs via `CrudTablePage` → KPI strip extension → list config rewrites (nhap-kho, ban-hang, tra-hang, chuyen-kho, cap-linh-kien, ds-tra-lk, ds-tra-lk-xac, dscaplk) → bulk workflows (Duyệt, Trả hàng) → line-item editors composing P2 `LineItemEditor` (receiving first as proof, then the 5) → prints → routes/nav wiring → full suite + gates.

## Implementation Steps

1. **`src/domains/warehouse/types.ts`**: define entities + line types per verified column sets (W1–W7, S1–S4). Include `IssuedPartUsage` (21 fields), `PartReturn` (18), `PartReturnXac` (20 incl. `maVanDon`), `InventoryRow` (superset of 20 cols), voucher + line types for the 6 editors.
2. **`src/domains/warehouse/mock-data.ts`**: list fetchers returning verified columns + `fetchInventory(kind, params)` with `{rows, kpi, total}`. **Kỳ carry-forward math (Finding 10, simplified)**: deterministic seeded per-Kỳ (nhập, xuất) deltas per product×kho keyed on P1's Kỳ lookup; `tonDauKy(ky) = tonDauKy(prevKy) + nhập(prevKy) − xuất(prevKy)`, `tonCuoiKy = tonDauKy + nhập − xuất` — a carry-forward function, **replacing the `inventory-mock.ts` `rngTK.int(10,200)` fabrication** (no materialized transaction-row ledger). `mock-data.test.ts` asserts the KPI trio is derived (opening chains across Kỳ, not random) + closing invariant + negative Tổng tồn possible.
3. **`src/domains/warehouse/mock-mutations.ts`**: `createReceiving`, `createCheckout`, `createSelling`/`updateSelling`, `createReturn`, `createMoving`(cung/khac), `duyetTraLK(ids)`, `traHang(ids, maVanDon)`, `thuXacLK(id)`, `traLK(id)`, `traLKFromTech(id, amount)`. Each mutates store + appends history where relevant.
4. **`src/components/finance/inventory-kpi-strip.tsx`**: add generic `KpiBox({label, value, tone})` and a `KpiTrio`; support negative values (no clamp/abs); tones green/blue/yellow.
5. **3 inventory grids via `CrudTablePage` config + page-level KPI/filter composition (Finding 11 + red-team H5)**: author `ton-kho`/`ton-kho-lk-xac`/`ton-kho-ky-thuat` `CrudConfig`s for the **table grids** (verified columns, row actions: Cập nhật modal, Xem chi tiết, NK/XK drill-down, rows-per-page 20–300). The **KPI trio and the Kỳ/cascade filter bar render at the page level** (as they already do — `XemTonKhoPage.tsx:52-58`), NOT inside `CrudConfig` (whose `FilterConfig.type` is only text/select/date-range, `crud-types.ts:46`, and has no KPI slot). Pages: `<InventoryKpiStrip .../>` + the Từ Kỳ/Đến Kỳ + Chi nhánh→Nhà kho→Ngăn chứa cascade + NSX/Model ServerAutocomplete filter bar, then `<CrudTablePage config={…}/>` for the grid — precedent `BangLuongPage.tsx:1` (aggregate page that hand-composes rather than forcing CrudTablePage). **Do NOT extend `CrudTablePage`/`CrudConfig` with a KPI-header + custom-filter system** (that recreates the cut host across 34 shared configs). **Data consistency (red-team Failure-Mode Finding 4): the table rows AND the KPI trio both read the SAME Kỳ carry-forward source** — the `fetchInventory` rows carry each product's `tonDauKy/tonCuoiKy` from the carry-forward; the KPI trio sums those same rows. No parallel `rngTK.int` rows left feeding one while the ledger feeds the other. `ky-thuat` config adds Kỹ thuật column + technician filter + `Trả linh kiện kho kỹ thuật` row modal.
6. **`nhap-kho.config.ts` + `NhapKhoPage`**: verified 12-col list; per-row In + Chi tiết; Tổng tiền info-box; remove Trạng thái; add Xuất ra Excel; `Thêm mới` → editor route.
7. **DSCapLK re-model**: rename `ThuHoiLKPage.tsx` → `DanhSachSuDungLinhKienPage.tsx`; 21-col config; `Tình trạng` action-button state machine; 15-status badge in Số phiếu hãng; 11 filters; 3 KPI boxes; Chi tiết modal; nav label → `Danh sách sử dụng linh kiện`.
8. **`ds-tra-lk.config.ts` + `DsTraLKPage`**: 18-col; bulk-select + Duyệt (bulk + per-row confirm `Bạn có chắn chắn Duyệt trả linh kiện?` → `duyetTraLK` → `Duyệt thành công!`); In Phiếu Trả (empty alert `Vui lòng chọn phiếu để in`); 9 filters; Tổng số LK KPI; Xuất ra Excel; remove Lý do + invented statuses.
9. **`DsTraLKXacPage` (NEW) + `ds-tra-lk-xac.config.ts`**: 20-col incl. Mã vận đơn; Trả hàng bulk (modal `Trả linh kiện`, empty alert `Vui lòng chọn phiếu để trả`, `traHang`); In BB Kỹ Thuật + In Phiếu Trả Hãng; 11 filters; Tổng số LK KPI; Xuất ra Excel. Add route `/quan-ly-kho/ds-tra-lk-xac` + nav child `Danh sách trả LK xác`.
10. **Stock-out list configs + pages**: `cap-linh-kien` (7-col, no checkbox, Tổng tiền box, Chi tiết + print, Tìm kiếm/Tìm chi tiết/Xuất Excel/Báo cáo lợi nhuận, 9 filters); `ban-hang` (verified cols, remove Trạng thái, 4 row actions incl. Thêm hình + Xuất kho print, bulk toolbar, 7 filters); `tra-hang` (5-col, replace refund model, Hình thức trả filter+column 4 values, Xuất Excel + Chi Tiết); `chuyen-kho` (10-col incl. Loại + Trạng thái lead, dual create buttons, replace invented statuses).
11. **Line-item editors (P2 template)**: build `receiving-editor` first (proves template), then `cap-linh-kien-editor`, `ban-hang-editor` (create+edit, [+] customer quick-create, price write-back), `tra-hang-editor` (type-driven two-stage), `chuyen-kho-cung-editor` + `chuyen-kho-khac-editor` (differing grids). Each: header fieldsets + line panel + grid + dual save + In. Wire routes.
12. **Prints**: 10 print documents via P2 print-window; exact titles/ids per specs.
13. **Modals**: `tra-linh-kien-tech-modal` (W4), `thu-xac-modal` + `tra-lk-modal` (DSCapLK), `voucher-detail-modal` (shared Chi tiết), Trả hàng bulk modal (DSTraLKXac), image-upload modal (Bán Hàng Thêm hình — reuse P4 gallery pattern if present, else client preview).
14. **Routes + nav** (`routes.ts`, `nav-config.tsx`, `routes/index.tsx`): add 6 editor routes, ban-hang edit, chuyen-kho dual create, DSTraLKXac page + nav child; rename DSCapLK nav label.
15. Run `npm run test` → green; then `npm run type-check && npm run lint && npm run build`; grep for removed invented fields (`ly_do`, `tong_tien_hoan`, `Chờ xác nhận` in chuyen-kho, receiving `trang_thai`) to confirm no dead refs.

## Success Criteria

- [ ] Every list renders its exact verified reference column set (spec tests green): NhapKho 12, XemTonKho 20, LK Xác 19, Kỹ Thuật 16, DSCapLK 21, DSTraLK 18, DSTraLKXac 20, CapLK 7, BánHàng 8, TrảHàng 5, ChuyểnKho 10.
- [ ] DSTraLKXac exists as a routed, nav-linked page; Trả hàng bulk + both prints + empty-selection alerts work with exact VI strings.
- [ ] 3 inventory views share one host, differ only by config; Từ Kỳ/Đến Kỳ period selects drive opening/closing stock; KPI trio shows (incl. negative Tổng tồn); NK/XK drill-downs + Cập nhật + Xem chi tiết present.
- [ ] DSTraLK Duyệt (bulk + per-row) flips Chờ duyệt→Đã duyệt via mock mutation with confirm/toast strings; DSCapLK Tình trạng action buttons drive Thu xác/Trả LK/In Tem state machine.
- [ ] All 6 full-page line-item editors create/update mock vouchers with line grids matching verified column sets; dual save + In work; Số phiếu auto; validations fire exact VI messages.
- [ ] Excel export present on every list that specs it; per-row prints open rendered documents.
- [ ] Invented models removed: TraHang refund fields, ChuyenKho invented statuses, NhapKho/BánHàng Trạng thái columns — grep clean.
- [ ] `npm run type-check && npm run lint && npm run test && npm run build` all clean.

## Risk Assessment

- **Scope is the largest phase (11 pages + 6 editors)** → mitigate by reusing what exists (`CrudTablePage` config-spread for the 3 inventory views, P2 `LineItemEditor` for the 6 editors — Finding 11, no new host/template); if timeboxed, ship lists + DSTraLKXac + inventory configs first (highest-severity gaps), then editors, then prints — each sub-batch leaves app green.
- **Period (Kỳ) math correctness (Finding 10)** → the Kỳ carry-forward is NEW this phase (`inventory-mock.ts` faked opening with `rngTK.int`). A deterministic carry-forward function over seeded per-Kỳ deltas (no materialized ledger — red-team simplification); `mock-data.test.ts` pins the chain + closing invariants. If P1's Kỳ lookup is thin, extend it in the P1 file, don't fudge in the fetcher.
- **6 divergent editors vs one component** → each editor composes P2's `LineItemEditor` directly with its own `lineColumns`/`headerFields`; the Trả Hàng two-stage flow stays its own component (no shared page-template forced over it — Finding 11).
- **Mock-write durability (Finding 6)** → created vouchers/slips live in module memory only; they vanish on ANY reload (not just `resetDemo`) — see plan.md §D5 + P2 durability note. Do NOT tell the user created vouchers persist. Tests exercise create→list-refetch within one session; no cross-reload persistence is claimed.
- **Rename ThuHoiLKPage** → keep route path stable (`thu-hoi-lk`), change only file/component/label to avoid breaking deep links + nav; update all imports.
- **Reference "Tìm kiếm vs Tìm chi tiết" dual mode** → local ships one result set; note as accepted deviation; keep both buttons wired to the same fetch unless a distinct line-detail shape is trivially available.
- **P1/P2 drift** (Kỳ picker API, editor template props, bulk-select) → 30-min interface check at phase start; fix in owner files, don't fork.
- **Rollback**: changes confined to `src/pages/quan-ly-kho/**`, `src/pages/xuat-kho/**`, `src/features/{inventory,warehouse-editors,stockout-editors,warehouse}/**`, `src/domains/warehouse/**`, `src/config/finance-tables/**`, `inventory-kpi-strip.tsx`, routes/nav — single phase-commit revert restores Phase-4 state; no localStorage key changes.

## Unresolved

1. **Tìm kiếm vs Tìm chi tiết** result-shape difference (voucher-level vs line-level) — reference renderer JS not mirrored; defaulting both to the same list. Confirm whether a distinct line-detail table is required before close.
2. **Chuyển Kho / Cấp LK per-row actions** — `/MovingInventory/List` and summary `/CheckOutForTech/List` captures had 0/summary rows; assuming edit/print/detail + confirm-receipt for Chuyển Kho (Chưa→Đã xác nhận). The receiving-confirmation action UI (who confirms, where) is inferred.
3. **Trả Hàng option-4 label conflict**: index filter says `Trả hàng từ kho`, editor says `Trả xác linh kiện` — plan treats editor as authoritative for creation, filter label kept for the list filter. Confirm the intended single label.
4. **DSCapLK Mục đích vs line-level purpose**: purpose appears both as a filter and a per-line concept; assuming a slip-level `Mục đích` column (as verified) driven by line context.
5. **Báo cáo lợi nhuận** (Cấp LK, Bán Hàng) export content/columns unknown (server-side) — ship as an `.xlsx` of the current list with a profit column placeholder; flag if real profit calc is required.
6. **Image-upload modal** (Bán Hàng Thêm hình) — reuse P4 repair gallery component if it generalizes; otherwise client-side FileReader preview only (D4).
