/**
 * Finance KPI mock — re-exports fetchFinanceKpi from finance-mock.
 * Kept as a separate module per spec deliverables list so import paths
 * match what hooks and pages expect.
 */
export { fetchFinanceKpi } from './finance-mock'
export type { FinanceKpiParams } from './finance-mock'
